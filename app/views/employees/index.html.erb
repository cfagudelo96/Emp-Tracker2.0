<h1>Empleados</h1>

<table class="table table-striped">
  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td>
          <p><strong><%= employee.name %></strong> <%= raw '<small>(Inactivo)</small>' unless employee.active %></p>
          <p><%= Area.find(employee.area_id) %> (<%= employee.position %>)</p>
          <p><%= Company.find(employee.company_id) %></p>
          <div class="pull-right">
            <%= link_to employee, class: 'btn btn-primary' do %>
              <%= fa_icon('info') %>
            <% end %>
            <%= link_to edit_employee_path(employee), class: 'btn btn-primary' do %>
              <%= fa_icon('pencil') %>
            <% end %>
            <%= link_to employee, method: :delete, data: { confirm: '¿Seguro que desea eliminar el empleado?' }, class: 'btn btn-primary' do %>
              <%= fa_icon('trash') %>
            <% end %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to new_employee_path, class: 'btn btn-primary' do %>
  <%= fa_icon('plus') %>
<% end %>
