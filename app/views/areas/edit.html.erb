<h1>Editando el área <%= "#{@area}" %></h1>

<div class="margin-bottom">
  <%= render 'form', area: @area %>
</div>

<%= link_to areas_path, class: 'btn btn-default' do %>
  <%= fa_icon('arrow-left') %>
<% end %>
