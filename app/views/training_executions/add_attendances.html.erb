<h1>Añadir asistencias a la formación</h1>

<%= form_for @training_execution, url: create_attendances_training_execution_path do |f| %>
  <div class="row">
    <div class="col-md-6">
      <div class="list-group" id="">
        <% @employees.each do |employee| %>
          <button type="button" onclick="selectEmployee(this, '<%= employee.name %>', <%= employee.id %>)" class="list-group-item list-group-item-compact"><%= employee %></button>
        <% end %>
      </div>
    </div>
    <div class="col-md-1">
      <div class="margin-bottom">
        <button class="btn btn-default"><%= fa_icon('angle-right') %></button>
      </div>
      <div>
        <button class="btn btn-default"><%= fa_icon('angle-left') %></button>
      </div>
    </div>
    <div class="col-md-5">
      <div class="list-group" id="selected-employees">
        <% @selected_employees.each do |employee| %>

        <% end %>
      </div>
    </div>
  </div>
<% end %>

